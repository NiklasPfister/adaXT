{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome","text":""},{"location":"#adaxt-fast-adaptable-and-extendable-trees-for-research","title":"adaXT - Fast adaptable and extendable trees for research","text":"<p>adaXT is a Python module for tree-based regression and classification that is fast, adaptable and extendable and aims to provide researchers a more flexible workflow when building tree-based models.</p>"},{"location":"#installation","title":"Installation","text":"<p>adaXT is available at pypi and can be installed by running <code>pip install adaXT</code>.</p>"},{"location":"decision_tree/criteria/criteria/","title":"The Criteria Class","text":"<p>This is the class used to evaluate the quality of a split while fitting a decision tree. It is accessed by: <pre><code>import adaXT.criteria\n</code></pre></p>"},{"location":"decision_tree/criteria/criteria/#adaXT.criteria.criteria.Criteria","title":"Criteria","text":"<p>Abstract class used as a parent for new Criteria functions</p>"},{"location":"decision_tree/criteria/criteria/#adaXT.criteria.criteria.Entropy","title":"Entropy","text":"<p>Calculates the Entropy given by $$ \\text{Entropy} = - \\sum_{i = 1}^n P[i] \\log_2 (P[i]), $$ where \\(P[i]\\) denotes the probability of randomly selecting an example in class i.</p>"},{"location":"decision_tree/criteria/criteria/#adaXT.criteria.criteria.Gini_index","title":"Gini_index","text":"<p>Calculates the gini index given by $$ \\text{Gini Index} = 1 - \\sum_{i=1}^n (P[i])^2, $$ where \\(P[i]\\) denotes the probability of an element being classified for a distinct class.</p>"},{"location":"decision_tree/criteria/criteria/#adaXT.criteria.criteria.Linear_regression","title":"Linear_regression","text":"<p>Calculates the impurity given by $$ \\text{Linear Regression} = \\sum_{i \\in indices} (Y[i] - \\theta_0 - \\theta_1 X[i, 0])^2. $$</p>"},{"location":"decision_tree/criteria/criteria/#adaXT.criteria.criteria.Squared_error","title":"Squared_error","text":"<p>Calculates the Squared error given by $$ \\text{Squared Error} = \\frac{\\sum_{i = 1}^n (Y[i] - \\mu_Y)^2}{n_{obs}}, $$ where \\(Y\\) denotes the outcome values in a node. \\(\\mu_Y\\) denotes the mean value in the node, and \\(n_{obs}\\) denotes the number of observations in the node.</p> <p>For a faster, but equivalent calculation, it is calculated using the formula $$ \\text{Squared Error} = \\frac{\\sum_{i = 1}^n Y[i]^2}{n_obs} - \\mu_Y^2 $$</p>"},{"location":"decision_tree/examples/creatingACriteria/","title":"How to create your own custom criteria function","text":"<p>In the following we explain how to create a custom criteria function by walking through the required steps. We start by creating .pyx file.</p>"},{"location":"decision_tree/examples/creatingACriteria/#creating-a-pyx-file","title":"Creating a .pyx file","text":"<p>In your working directory create a .pyx file in which you first import the Criteria \"super\" class and then define the new custom Criteria class which inherits from the imported Criteria class. An example of the skeleton of the the Linear_regression Criteria file is given below. <pre><code>from adaXT.criteria cimport Criteria\n\ncdef class Linear_regression(Criteria):\n    # Your implementation here\n</code></pre> The class type of Criteria is a cdef class, which works in much of the same fashion as a regular Python class, but with faster performance. To read more on cdef classes checkout Cython extension types.</p> <p>Now for the criteria to work with adaXT you have to implement the impurity method inherited from the Criteria class. The impurity method has to follow the type specified within criteria.pxd, which is as follows. <pre><code>    cpdef double impurity(self, int[:] indices):\n</code></pre> Here, the indices refer to the sample indices for which the impurity value should be computed. To access the feature and response you can make use of <code>self.x</code> and <code>self.y</code>, respectively. More specifically, <code>self.x[indices]</code> and <code>self.y[indices]</code> are the feature and response samples for which the impurity needs to be computed. With this in place you should be able to build the your custom Cython file. This can be done either by following building cython code or by following the steps below.</p> <p>Now that you have implemented your custom criteria function, we explain how to use it in a simple setting.</p>"},{"location":"decision_tree/examples/creatingACriteria/#using-a-custom-criteria","title":"Using a custom criteria","text":"<p>Basically you can now create your .py file in which you fit a decision tree using the new custom criteria within the same folder where you created the .pyx. For this to work there are a few small things left to do. In order to not manually recompile the Cython file every time you make changes, you can automate the compilation within the Python file by telling Cython to comile the Cython source file, whenever you run the file: <pre><code>from adaXT.decision_tree import DecisionTree\nimport pyximport; pyximport.install()\nimport &lt;your .pyx filename&gt;\n</code></pre> Now you can access the new criteria within the Python file like you would do with any other criteria function:</p> <p><pre><code>import &lt;your .pyx filename&gt;\nimport numpy as np\n\nn = 100\nm = 4\n\nX = np.random.uniform(0, 100, (n, m))\nY = np.random.uniform(0, 10, n)\ntree = DecisionTree(\"Regression\", criteria=&lt;your .pyx filename&gt;.&lt;your cdef class&gt;, max_depth=3)\ntree.fit(X, Y)\n</code></pre> We now go through a detailed example in which we construct the linear regression criteria.</p>"},{"location":"decision_tree/examples/creatingACriteria/#a-detailed-example-linear-regression","title":"A detailed example: Linear regression","text":"<p>The general idea of the linear regression criteria is to fit a linear function on the first feature with the y value as the response, that is, $$ L = \\sum_{i \\in I} (Y[i] - \\theta_0 - \\theta_1 X[i, 0])^2 \\ $$ $$ \\theta_1 = \\frac{\\sum_{i \\in I} (X[i, 0] - \\mu_X)(Y[i] - \\mu_Y)}{\\sum_{i \\in I} (X[i, 0] - \\mu_X)^2} \\ $$ $$ \\theta_0 = \\mu_Y - \\theta_1 \\mu_X, $$ where \\(I\\) denotes the indices of the samples within a given node, \\(X\\) is the feature matrix, \\(Y\\) is the response vector, \\(\\mu_X\\) is mean vector of the columns of \\(X\\) and \\(\\mu_Y\\) is the mean of \\(Y\\).</p> <p>When creating a new criteria function we import and define the class as described above. This leads to: <pre><code>from adaXT.criteria cimport Criteria\n\ncdef class Linear_regression(Criteria):\n</code></pre></p>"},{"location":"decision_tree/examples/creatingACriteria/#calculating-the-mean","title":"Calculating the mean","text":"<p>Now although we have provided a mean function within the adaXT.decision_tree.crit_helpers, in this specific example we need to calculate multiple means, and there is no reason to do two passes over the same indices \\(I\\), so we create a custom mean method in Cython:</p> <p><pre><code># Custom mean function, such that we don't have to loop through twice.\ncdef (double, double) custom_mean(self, int[:] indices):\n    cdef:\n        double sumX, sumY\n        int i\n        int length = indices.shape[0]\n    sumX = 0.0\n    sumY = 0.0\n    for i in range(length):\n        sumX += self.x[indices[i], 0]\n        sumY += self.y[indices[i]]\n\n    return ((sumX / (&lt;double&gt; length)), (sumY / (&lt;double&gt; length)))\n</code></pre> You might notice that the syntax is a little different than for default Python. In general you can write default Python within the cdef class, however the runtime can be significantly decreased by adding type definitions and other Cython magic. If you wish to learn more about the Cython language be sure to check out the Cython documentation.</p> <p>Furthermore, note that you can freely create any new method within the custom Criteria class criteria even if it is not defined in the standard parent Criteria class. Therefore you are not limited by the parent class and can freely extend it as long as you do not overwrite the <code>evaluate_split</code> (unless that is your intention).</p>"},{"location":"decision_tree/examples/creatingACriteria/#calculating-theta","title":"Calculating theta","text":"<p>Now that we have a method for getting the mean, we can create a method for calculating the theta values, such that our code is more manageable.</p> <p><pre><code>cdef (double, double) theta(self, int[:] indices):\n    \"\"\"\n    Calculate theta0 and theta1 used for a Linear Regression\n    on X[:, 0] and Y\n    ----------\n\n    Parameters\n    ----------\n    indices : memoryview of NDArray\n        The indices to calculate\n\n    Returns\n    -------\n    (double, double)\n        where the first element is theta0 and second element is theta1\n    \"\"\"\n    cdef:\n        double muX, muY, theta0, theta1\n        int length, i\n        double numerator, denominator\n        double X_diff\n\n    length = indices.shape[0]\n    denominator = 0.0\n    numerator = 0.0\n    muX, muY = self.custom_mean(indices)\n    for i in range(length):\n        X_diff = self.x[indices[i], 0] - muX\n        numerator += (X_diff)*(self.y[indices[i]]-muY)\n        denominator += (X_diff)*X_diff\n    if denominator == 0.0:\n        theta1 = 0.0\n    else:\n        theta1 = numerator / denominator\n    theta0 = muY - theta1*muX\n    return (theta0, theta1)\n</code></pre> Again the majority of the Cython is not mandatory but speeds up the code. On line 26 we access our previously defined custom mean function, which returns the mean of the \\(X\\) indices and the mean of the \\(Y\\) indices as described above. Then at line 27 we loop over all the indices a second time and calculate \\(\\sum_{i \\in I} (X[i, 0] - \\mu_X) (Y[i] - \\mu_Y)\\) and \\(\\sum_{i \\in I} (X[i, 0] - \\mu_X)^2\\) which are the numerator and denominator, respectively. These are the two values used to calculate \\(\\theta_1\\). Further on line 31 we check to make sure that the denominator is not 0.0, this ensures that we can consider the underidentified case separately and do not divide by zero by accident. If the denominator is zero, we simply set \\(\\theta_1\\) to 0.0 as this will give an L value of 0 in the end. We finish off by returning the two values \\(\\theta_0,\\theta_1\\).</p>"},{"location":"decision_tree/examples/creatingACriteria/#the-impurity-function","title":"The impurity function","text":"<p>Finally, we reach the most important step, which is to create the impurity function. <pre><code>cpdef double impurity(self, int[:] indices):\n    cdef:\n        double step_calc, theta0, theta1, cur_sum\n        int i, length\n\n    length = indices.shape[0]\n    theta0, theta1 = self.theta(indices)\n    cur_sum = 0.0\n    for i in range(length):\n        step_calc = self.y[indices[i]] - theta0 - theta1 * self.x[indices[i], 0]\n        cur_sum += step_calc*step_calc\n    return cur_sum\n</code></pre> Making sure the impurity method has the required signature, we simply calculate L as described and return its value. One important thing to note is that <code>cur_sum</code> is defined on line 3 to have type double, because the impurity function is defined to have a double as return value. When creating the impurity method you must ensure this return type.</p>"},{"location":"decision_tree/examples/creatingACriteria/#finishing-up","title":"Finishing up","text":"<p>And that is it. You have now created your first Criteria function, and can freely use it within your own Python code. If you use the method described above to compile the Cython file every time the Python file is run leads to code that looks like this: <pre><code>from adaXT.decision_tree import DecisionTree\nfrom adaXT.decision_tree.tree_utils import plot_tree\nimport matplotlib.pyplot as plt\nimport pyximport; pyximport.install()\nimport testCrit\nimport numpy as np\n\nn = 100\nm = 4\n\n\nX = np.random.uniform(0, 100, (n, m))\nY = np.random.uniform(0, 10, n)\ntree = DecisionTree(\"Regression\", testCrit.Linear_regression, max_depth=3)\ntree.fit(X, Y)\n\nplot_tree(tree)\nplt.show()\n</code></pre> This just creates a regression tree with the newly created custom Linear_regression Criteria function, specifies the <code>max_depth</code> to be 3 and then plots the tree using both our plot_tree and the matplotlib. The full source code used within this article can be found here.</p>"},{"location":"decision_tree/examples/creatingATree/","title":"Decision trees","text":"<p>A decision tree is a predictive regression and classification model, which trained on previous data, can be used to predict future responses. Decisions trees have a tree structure, where each internal node splits the dataset based on some threshold value for a given feature index.</p> <p>The DecisionTree class is used when creating both a regression and classification tree.</p>"},{"location":"decision_tree/examples/creatingATree/#classification-trees","title":"Classification trees","text":"<p><pre><code>from adaXT.decision_tree import DecisionTree\nfrom adaXT.criteria import Gini_index\nX = [[0, 0], [1, 0]]\nY = [0, 1]\ntree = DecisionTree(\"Classification\", criteria=Gini_index)\ntree.fit(X, Y)\n</code></pre> In the example above we are creating and fitting a classification tree with the Gini Index criteria function using the X and Y data specified as training data.</p>"},{"location":"decision_tree/examples/creatingATree/#regression-trees","title":"Regression trees","text":"<p>Regression trees work similar to classification trees, with one small difference demonstrated by the following example: <pre><code>from adaXT.decision_tree import DecisionTree\nfrom adaXT.criteria import Squared_error\nX = [[0, 0], [1, 0]]\nY = [0, 1]\ntree = DecisionTree(\"Regression\", criteria=Squared_error)\ntree.fit(X, Y)\n</code></pre> You have to specify that you are now using a regression tree instead of a classification tree, and adaXT takes care of the rest. The reason for this specification is that regression and classification trees need to have some small differences in saved objects. This is for example relevant when making predictions as shown next.</p>"},{"location":"decision_tree/examples/creatingATree/#using-the-fitted-tree","title":"Using the fitted tree","text":""},{"location":"decision_tree/examples/creatingATree/#prediction","title":"Prediction","text":"<p>After the fitting we can predict new values using the predict method of the tree. <pre><code>result = tree.predict(np.array([5, 0]))\nprint(result) # prints [1.]\n</code></pre> This works for both regression and classification trees, but the output is different. Both tree types first locate the nodes in which the new samples fall based on their fitted structure. The classification tree then calculates the proportion of each class within those nodes, chooses the class with the highest proportion and returns that value as the prediction. In contrast the regression tree only calculates the mean value of all samples within each of those node and returns that as the prediction.</p>"},{"location":"decision_tree/examples/creatingATree/#prediction-probability","title":"Prediction probability","text":"<p>For classification it can useful to get a notion of classification probability for each of the possible classes instead of the predicted class label. With classification trees a commonly used option is to output the frequency each class has in the leaf nodes. In adaXT this can be done using the predict_proba function.</p> <p>It works in much the same fashion as the prediction function, but importantly only applies to classification trees. Given the above fitted classification tree, we have the following: <pre><code>result = tree.predict_proba([5, 0])\nprint(result) # prints  array([[0., 1.]]\n</code></pre> Here the function returns a numpy arrays with the probability of an element being either of the classes. To get the list of classes (in the correct order) use .classes on the DecisionTree.</p>"},{"location":"decision_tree/tree/DecisionTree/","title":"The Decision Tree","text":"<p>Accessible by: <pre><code>from adaXT.decision_tree import DecisionTree\n</code></pre> </p>"},{"location":"decision_tree/tree/DecisionTree/#adaXT.decision_tree.DecisionTree.DecisionTree","title":"DecisionTree","text":"<pre><code>DecisionTree(tree_type, criteria, max_depth=sys.maxsize, impurity_tol=0, min_samples_split=1, min_samples_leaf=1, min_improvement=0, max_features=None, splitter=None)\n</code></pre> <p>Attributes:</p> Name Type Description <code>max_depth</code> <code>int</code> <p>maximum depth of the tree</p> <code>tree_type</code> <code>str</code> <p>Which type of tree it is</p> <code>leaf_nodes</code> <code>list[LeafNode]</code> <p>a list of all the leaf nodes in the tree</p> <code>root</code> <code>Node</code> <p>the root Node of the tree</p> <code>n_nodes</code> <code>int</code> <p>the number of nodes in the tree</p> <code>n_features</code> <code>int</code> <p>the number of features of the training data</p> <code>n_classes</code> <code>int</code> <p>the number of classes of the training data, 0 for Regression Tree</p> <code>n_obs</code> <code>int</code> <p>the number of observations used for training</p> <code>classes</code> <code>ndarray</code> <p>the different classes in the tree given a Classification tree</p> <p>Parameters:</p> Name Type Description Default <code>tree_type</code> <code>str</code> <p>Classification or Regression</p> required <code>criteria</code> <code>Type[Criteria]</code> <p>The Criteria class to use, should be of the type Criteria implemented by AdaXT</p> required <code>max_depth</code> <code>int</code> <p>maximum depth of the tree, by default maximum system size</p> <code>maxsize</code> <code>impurity_tol</code> <code>float</code> <p>the tolerance of impurity in a leaf node, by default 0</p> <code>0</code> <code>min_samples_split</code> <code>int</code> <p>the minimum amount of samples in a split, by default 1</p> <code>1</code> <code>min_samples_leaf</code> <code>int</code> <p>the minimum amount of samples in a leaf node, by default 1</p> <code>1</code> <code>min_improvement</code> <code>float</code> <p>the minimum improvement gained from performing a split, by default 0</p> <code>0</code> <code>max_features</code> <code>None</code> <p>the number of features to consider when looking for a split</p> <code>None</code> <code>splitter</code> <code>Splitter | None</code> <p>Splitter class if None uses premade Splitter class</p> <code>None</code>"},{"location":"decision_tree/tree/DecisionTree/#adaXT.decision_tree.DecisionTree.DecisionTree.fit","title":"fit","text":"<pre><code>fit(X, Y, sample_indices=None, sample_weight=None)\n</code></pre> <p>Function used to fit the data on the tree using the DepthTreeBuilder</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>array-like object</code> <p>feature values, will internally be converted to np.ndarray with dtype=np.float64</p> required <code>Y</code> <code>array-like object</code> <p>response values, will internally be converted to np.ndarray with dtype=np.float64</p> required <code>sample_indices</code> <code>array-like object</code> <p>specific indices of the dataset you wish to use</p> <code>None</code> <code>sample_weight</code> <code>ndarray | None</code> <p>np.ndarray of shape (n_samples,) currently only supports weights in {0, 1}</p> <code>None</code>"},{"location":"decision_tree/tree/DecisionTree/#adaXT.decision_tree.DecisionTree.DecisionTree.get_leaf_matrix","title":"get_leaf_matrix","text":"<pre><code>get_leaf_matrix(scale=False)\n</code></pre> <p>Creates NxN matrix, where N is the number of observations. If A_{i,j} = 1 then i and j are in the same LeafNode, otherwise 0. If they are scaled, then A_{i,j} is instead scaled by the number of elements in the leaf node.</p> <p>Parameters:</p> Name Type Description Default <code>scale</code> <code>bool</code> <p>whether to scale the entries, by default False</p> <code>False</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>NxN matrix</p>"},{"location":"decision_tree/tree/DecisionTree/#adaXT.decision_tree.DecisionTree.DecisionTree.predict","title":"predict","text":"<pre><code>predict(X)\n</code></pre> <p>Works in two ways depending on if the tree is a Classification or Regression tree.</p> Classification: <p>Returns the class with the highest proportion within the final leaf node</p> Regression: <p>Returns the mean value of the outcomes within the final leaf node.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>ndarray</code> <p>(N, M) numpy array with features to predict</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>(N) numpy array with the prediction</p>"},{"location":"decision_tree/tree/DecisionTree/#adaXT.decision_tree.DecisionTree.DecisionTree.predict_leaf_matrix","title":"predict_leaf_matrix","text":"<pre><code>predict_leaf_matrix(X, scale=False)\n</code></pre> <p>Creates NxN matrix, where N is the number of observations in X. If A_{i,j} = 1 then i and j are in the same leafnode, otherwise 0. If they are scaled, then A_{i,j} is instead scaled by the number of elements in the leaf node.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>ndarray</code> <p>new values to be fitted</p> required <code>scale</code> <code>bool</code> <p>whether to scale the entries, by default False</p> <code>False</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>NxN matrix</p>"},{"location":"decision_tree/tree/DecisionTree/#adaXT.decision_tree.DecisionTree.DecisionTree.predict_proba","title":"predict_proba","text":"<pre><code>predict_proba(X)\n</code></pre> <p>Predicts a probability for each response for given X values. Only useable by the Classification Tree.</p> <p>Parameters:</p> Name Type Description Default <code>X</code> <code>ndarray</code> <p>(N, M) numpy array with features to predict</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Returns an np.ndarray with the the probabilities for each class per observation in X, the order of the classes corresponds to that in the attribute classes.</p>"},{"location":"decision_tree/utils/tree_utils/","title":"Tree Utilities","text":"<p>Accesible by <pre><code>import adaXT.decision_tree.tree_utils\n</code></pre> </p>"},{"location":"decision_tree/utils/tree_utils/#adaXT.decision_tree.tree_utils.plot_node","title":"plot_node","text":"<pre><code>plot_node(ax, node, node_positions)\n</code></pre> <p>Helper function used to plot each node of a DecisionTree</p> <p>Parameters:</p> Name Type Description Default <code>ax</code> <code>Axes</code> <p>axes to plot on</p> required <code>node</code> <code>Node</code> <p>node type of a tree</p> required <code>node_positions</code> <code>tuple</code> <p>(left_child position, right_child position, nodes own position)</p> required"},{"location":"decision_tree/utils/tree_utils/#adaXT.decision_tree.tree_utils.plot_tree","title":"plot_tree","text":"<pre><code>plot_tree(tree)\n</code></pre> <p>Generates the tree as a subplot of plt. To show the plot, the user needs to call mathplotlib.pyplot.show().</p> <p>Parameters:</p> Name Type Description Default <code>tree</code> <code>DecisionTree</code> <p>the tree to plot</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>the figure of the subplot</p> <code>Axes</code> <p>the axes of the subplot</p>"}]}