name: Build

on:
  workflow_dispatch:

  pull_request:
    branches:
      - "main"

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, windows-2019, macos-11]

    steps:
      - uses: actions/checkout@v4

      - name: Build wheels
        uses: pypa/cibuildwheel@v2.16.5
        with:
          package-dir: .
          output-dir: wheelhouse
          config-file: "{package}/pyproject.toml"

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ matrix.os }}
          path: ./wheelhouse/*.whl

  # commit_wheels:
  #   name: commit_wheels
  #   runs-on: ubuntu-latest
  #   needs: build_wheels
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         token: ${{ secrets.ADAXT_ACTIONS }}
  #         ref: ${{ github.head_ref }}
  #
  #     - name: Download pipx and make source distribution
  #       run: |
  #         sudo apt install pipx
  #         rm -rf dist/ || true # Remvove if it exists
  #         pipx run build --sdist
  #
  #     - uses: actions/download-artifact@v4
  #       with:
  #         pattern: cibw-wheels-*
  #         merge-multiple: true
  #         path: dist/
  #
  #     - name: commit to branch
  #       run: |
  #         git config --global user.name 'Niklas Andreas Pfister'
  #         git config --global user.email 'np@math.ku.dk'
  #         git commit -am "Added wheels in dist/"
  #         git push
